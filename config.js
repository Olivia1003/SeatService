const config = {

    port: 3306,

    database: {
        DATABASE: 'seat',
        USERNAME: 'root',
        PASSWORD: '911916',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config